% by ziyuang@gmail.com

\documentclass{article}
\usepackage{amsmath,amssymb}
\usepackage{xltxtra,xunicode}
\usepackage{fontspec}

\setmainfont[BoldFont=SimHei]{SimSun}
\XeTeXlinebreaklocale "zh"
\XeTeXlinebreakskip = 0pt plus 1pt

\begin{document}
\begin{enumerate}
\item
%\begin{problem}
设$E$为数域，$F\subset E$，且$E$作为$F$上的线性空间时维数为$m$. 设$V$为$E$上的$n$维线性空间，证明：$V$作为$F$上的线性空间时维数为$mn$.
%\end{problem}
\item
%\begin{problem}
设$f$是数域$F$上的线性空间$M_n(F)$到$F$的线性映射，并满足$f(I)=n$，且对任意矩阵$A,B\in M_n(F)$有$f(AB)=f(BA)$。证明：$f=\mathrm{tr}$.
%\end{problem}
\item
%\begin{problem}
设$A\in M_n(F)$，$\mathrm{rank}(A)<n$，且$A=B_1B_2\cdots B_k$，其中$B_i^2=B_i\in M_n(F)\,(i=1,2,\cdots,k)$. 证明：$\mathrm{rank}(I-A)\le k(n-\mathrm{rank}(A))$.
%\end{problem}
\item
%\begin{problem}
设$A\in F^{m\times n}$. 若对任意$n$维向量$b\in F^n$，线性方程组$Ax=b$有解，证明：$\mathrm{rank}(A)=m$.
%\end{problem}
\item
%\begin{problem}
设$f(x)=x^3$，$g(x)=(1-x)^2$.
\begin{enumerate}
\item
求$u(x),v(x)$，使$(f(x),g(x))=u(x)f(x)+v(x)g(x)$;
\item
设$r_1(x)=x+2$，$r_2(x)=1$. 求一多项式$h(x)$使下列同余方程组成立：
\begin{align*}
h(x)\equiv& r_1(x)\mod{f(x)}\\
h(x)\equiv& r_2(x)\mod{g(x)}
\end{align*}
\end{enumerate}
%\end{problem}
\item
%\begin{problem}
设$\sigma$是数域$F$上线性空间$V$上的线性变换. $W$是$\sigma$的不变子空间. $\lambda_1,\cdots,\lambda_m$是$\sigma$的两两不同的特征根，$\alpha_1,\cdots,\alpha_m$分别是属于$\lambda_1,\cdots,\lambda_m$的根向量. 若$\alpha=\alpha_1+\cdots+\alpha_m\in W$，证明$\alpha_i\in W\,(i=1,\cdots,m)$.
%\end{problem}
\item
%\begin{problem}
设复矩阵$\displaystyle A=
\begin{pmatrix}
0 & -2 & 3 & 2\\
1 & 1 & -1 & -1\\
0 & 0 & 2 & 0\\
1 & -1 & 0 & 1
\end{pmatrix}
$. 求$A$的若当标准型和最小多项式.
%\end{problem}
\item
%\begin{problem}
设$W$为下列实线性方程组的解空间. 分别求$W$和$W^\perp$的一个标准正交基：
\begin{align*}
2x_1+x_2-x_3+x_4=&0\\
x_1+x_2-x_3=&0
\end{align*}
%\end{problem}
\item
%\begin{problem}
设实矩阵$\displaystyle A=\begin{pmatrix}
3 & -2 & -4\\
-2 & 6 & -2\\
-4 & -2 & 3
\end{pmatrix}$. 求正交矩阵$P$使$P^{-1}AP$为对角矩阵.
%\end{problem}
\item
%\begin{problem}
设$A,B$都是$n$阶实矩阵，其中$A$正定，$B$半正定. 证明：$\det(A+B)\ge\det A$.
%\end{problem}
\end{enumerate}
\end{document}